---
description: GitHub PR workflow so reviews have a real branch diff
alwaysApply: true
---

# PR workflow

- Implement work on a feature branch, never commit straight to `master`.
- Open a PR so `/review-bugbot` and `/review-security` compare against `master`.
- Do not merge until the user has run the required `/` reviews (or explicitly skipped them).
